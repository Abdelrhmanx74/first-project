{
  "name": "first-project",
  "private": true,
  "packageManager": "pnpm@8.8.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "install-all": "pnpm -w install",
  "dev:all": "concurrently \"pnpm --dir ./apps/backend run start:dev\" \"pnpm --dir ./apps/frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
